import math\n\nfrom app.cpd import build_cpd\n\n\ndef test_cpd_reproduces_published_upper_limit_example():\n    # Biermann/Potente example: mean TSD 7.6 h, ±2 SD = 2.8 h,\n    # maximum TSD 7 h. Published CPD is approximately 06:15–08:40\n    # for a 13:15 measurement.\n    result = build_cpd(mean_hours=7.6, sd_hours=1.4, lower_hours=0.0, upper_hours=7.0)\n    assert math.isclose(result.cpd_high_hours, 7.0, abs_tol=1e-8)\n    assert math.isclose(result.cpd_low_hours, 4.5693, abs_tol=0.01)\n\n\ndef test_cpd_reproduces_published_found_dead_example():\n    # Found dead at 08:02 with measurement at 13:15 => minimum TSD 5 h 13 min.\n    result = build_cpd(\n        mean_hours=7.6,\n        sd_hours=1.4,\n        lower_hours=5.0 + 13.0 / 60.0,\n        upper_hours=7.0,\n    )\n    assert math.isclose(result.cpd_high_hours, 7.0, abs_tol=1e-8)\n    assert math.isclose(result.cpd_low_hours, 5.3938, abs_tol=0.01)\n    assert math.isclose(result.cpd_width_hours, 1.6062, abs_tol=0.01)\n\n\ndef test_unconditioned_gaussian_returns_approximately_two_sd_interval():\n    result = build_cpd(mean_hours=10.0, sd_hours=1.5, lower_hours=0.0, upper_hours=math.inf)\n    # Because TSD >= 0 is formally conditioned on, the result is numerically\n    # indistinguishable from ±2 SD when the lower boundary is deep in the tail.\n    assert math.isclose(result.cpd_low_hours, 7.0, abs_tol=0.01)\n    assert math.isclose(result.cpd_high_hours, 13.0, abs_tol=0.01)\n