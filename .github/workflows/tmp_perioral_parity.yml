name: Temporary perioral parity

on:
  push:
    branches:
      - refactor-tanatology-keys
    paths:
      - .tmp_perioral_parity.py

permissions:
  contents: write

jobs:
  revise:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: refactor-tanatology-keys
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Install dependencies
        run: python -m pip install -r requirements.txt

      - name: Apply perioral parity
        run: python .tmp_perioral_parity.py

      - name: Compile Python sources
        run: python -m compileall -q app Stima_epoca_decesso.py pages

      - name: Run regression tests
        run: python -m unittest discover -s tests -v

      - name: Commit revision and remove temporary files
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git rm .github/workflows/tmp_perioral_parity.yml .tmp_perioral_parity.py
          git add app/perioral_single_grid.py app/special_heading_ui.py
          git commit -m "Uniforma griglia peribuccale alla sopraciliare"
          git push origin HEAD:refactor-tanatology-keys
