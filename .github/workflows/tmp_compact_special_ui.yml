name: Temporary compact special UI

on:
  push:
    branches:
      - refactor-tanatology-keys
    paths:
      - .tmp_compact_special_ui_trigger

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: refactor-tanatology-keys
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Install dependencies
        run: python -m pip install -r requirements.txt

      - name: Apply UI update
        run: python .tmp_compact_special_ui.py

      - name: Compile Python sources
        run: python -m compileall -q app Stima_epoca_decesso.py pages

      - name: Run regression tests
        run: python -m unittest discover -s tests -v

      - name: Commit update and remove temporary files
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git rm .github/workflows/tmp_compact_special_ui.yml .tmp_compact_special_ui.py .tmp_compact_special_ui_trigger
          git add app/full_factor_panel.py app/special_heading_ui.py app/supra_single_grid.py app/perioral_single_grid.py
          git commit -m "Mostra nota peso e compatta dati aggiuntivi mobile"
          git push origin HEAD:refactor-tanatology-keys
